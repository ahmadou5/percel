[build]
builder = "nixpacks"
buildCommand = "pnpm build && npx prisma migrate deploy"

[deploy]
startCommand = "pnpm start"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
